---
- name: Setup network for host
  import_tasks: network.yml
  tags:
    - common
    - network

- name: Ensuring user {{ app_user }} exists
  import_tasks: user.yml
  tags:
    - common
    - user

# TODO: maybe install some useful package
# - name: Ensuring pip and python packages has installed on host
#   tags:
#     - common
#     - pip
